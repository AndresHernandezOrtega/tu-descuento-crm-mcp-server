# ============================================
# VARIABLES DE ENTORNO PARA DOCKER HOST
# ============================================
# Este archivo debe estar en el MISMO directorio que docker-compose.yml
# NO dentro del contenedor Docker
# Copiar a .env y ajustar valores reales
# ============================================

# Puerto externo del host (el contenedor siempre usa 3000 internamente)
PORT=3000

# URL base de la API de Tu Descuento (PRODUCCIÓN - REQUERIDO)
TUDESCUENTO_API_URL=https://api.tudescuento.com.co

# API Key de Tu Descuento (PRODUCCIÓN - REQUERIDO)
# IMPORTANTE: Usar credenciales de producción, no de desarrollo
TUDESCUENTO_API_KEY=your_production_api_key_here

# Configuración del servidor MCP (OPCIONALES)
MCP_SERVER_NAME=tudescuento-mcp-server
MCP_SERVER_VERSION=1.0.0

# Nivel de log (info, warn, error - NO usar debug en producción)
LOG_LEVEL=info

# CORS Origins permitidos (IMPORTANTE: Especificar dominios exactos)
# Separar múltiples dominios con comas, SIN espacios extra
CORS_ORIGINS=https://n8n.tudescuento.com.co,https://app.tudescuento.com.co

# ============================================
# NOTAS IMPORTANTES
# ============================================
# 1. Este archivo .env es para el HOST, no el contenedor
# 2. Docker Compose lee estas variables y las pasa al contenedor
# 3. NUNCA subir este archivo a Git (ya está en .gitignore)
# 4. Las variables TUDESCUENTO_API_URL y TUDESCUENTO_API_KEY son OBLIGATORIAS
# 5. Si faltan, el contenedor fallará al iniciar con error claro
